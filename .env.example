# Copier ce fichier en `.env.local` et renseigner les valeurs du projet
# Supabase (Dashboard → Settings → API). Sans ces valeurs, le site tourne
# en mode démonstration sur les données de src/lib/data/seed.ts.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
